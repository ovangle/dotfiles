set et ts=2 sw=2

set nocompatible

" Put swapfiles into a tmp directory
set backupdir=~/.tmp
set directory=~/.tmp
set undodir=~/.tmp

" Use the system clipboard for yank + pull
set clipboard=unnamed

set relativenumber

